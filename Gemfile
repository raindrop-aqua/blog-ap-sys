source "https://rubygems.org"

# テーマ。jekyll 本体・jekyll-paginate・seo-tag・archives・sitemap・include-cache は
# このgemのランタイム依存として入り、Jekyllが自動でrequireするため個別指定は不要
gem "jekyll-theme-chirpy", "~> 7.6"

# Ruby 3.0+ では webrick が標準ライブラリから外れたため明示的に追加（jekyll serve用）
gem "webrick", "~> 1.8"
